[build]
  functions = "netlify/functions"

# 配置函数超时时间 (单位：秒)
# 免费计划最大 60 秒，Pro 计划最大 900 秒
[functions]
  node_bundler = "esbuild"
  timeout = 60 

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200